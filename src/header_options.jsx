import React from "react";

// import * as React from "react";

const HeaderOptions = ({ Icon }) => {
  return (
    <div className=" ">
      <Icon />
    </div>
  );
};

export default HeaderOptions;
